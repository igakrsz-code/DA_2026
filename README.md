# SQL Exercises

## Exercise 1 — Items and Customers
Queries on the `public` database covering ORDER BY, WHERE, and LIMIT.

## Exercise 2 — dvdrental
Queries on the `dvdrental` sample database covering SELECT, aliases, DISTINCT,
JOINs, subqueries, LIKE, IN, and window functions.
