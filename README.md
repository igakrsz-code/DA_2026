# Deck of Cards – Python OOP Exercise

## Project Description

This project demonstrates basic **Object-Oriented Programming (OOP)** concepts in Python by implementing a deck of playing cards.

The program creates a standard deck of **52 cards**, allows the deck to be **shuffled**, and enables dealing cards one by one.

The project practices:

* Classes and objects
* Methods
* Lists as data structures
* Randomization using `random.shuffle()`

---

## OOP Concepts

### What is a class?

A class is a blueprint used to create objects. It defines the attributes and methods that objects created from it will have.

### What is an instance?

An instance is a specific object created from a class.

### What is encapsulation?

Encapsulation is the practice of bundling data and methods together inside a class and restricting direct access to some components.

### What is abstraction?

Abstraction means hiding complex implementation details and exposing only the necessary parts of the program.

### What is inheritance?

Inheritance allows a class to inherit attributes and methods from another class.

### What is multiple inheritance?

Multiple inheritance allows a class to inherit from more than one parent class.

### What is polymorphism?

Polymorphism allows the same method name to behave differently depending on the object using it.

### What is Method Resolution Order (MRO)?

MRO is the order Python follows to determine which method to use when multiple classes are involved in inheritance.

---

## Features

* Create a standard 52-card deck
* Shuffle the deck
* Deal cards one at a time
* Remove dealt cards from the deck

---

## How to Run

Clone the repository:

```
git clone https://github.com/yourusername/deck-of-cards-oop.git
```

Run the program:

```
python deck.py
```

---

## Example Output

```
7 of Hearts
K of Spades
2 of Clubs
```

---

## Technologies

* Python
* Object-Oriented Programming
